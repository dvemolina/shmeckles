import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shmeckles.fintrack',
  appName: 'Personal Finance Tracker',
  webDir: 'build'
};

export default config;
